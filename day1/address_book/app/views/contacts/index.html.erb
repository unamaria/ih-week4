<h2>Contacts</h2>
<ul>
	<%= @contacts.each do |contact| %>
	<li>
		<%= link_to contact.name, contact_path(contact) %>
<!-- 		<%= contact.address %>
		<%= contact.phone_number %>
		<%= contact.email %>
 -->	</li>
	<% end %>
</ul>